# Project Context

- This repository is the Flutter mobile client for the existing Django backend repository: `django-auth-core`.
- The current phase is environment and bootstrap setup only.
- Target mobile platforms are iPhone (iOS) and Android.
- The primary local development machine is macOS.
- Backend API integration is intentionally deferred to a later phase.
